Continue Mandi360pro from previous. Add Farmer Invoice Module:
- Sub-Modules: Dhada Book, Farmer Invoice (tabbed interface).
- UI/UX: List-first table view with filter/search (e.g., FarmerName/Product), Add button opens full-screen form (centered, clean layout), action buttons (Add Ctrl+N, Edit Ctrl+E, Delete Ctrl+D, Save Ctrl+S, Print Ctrl+P), row fade-in.
- Workflow: 
  - Add → Click Add → Full-screen form opens → Fill → Save.
  - Edit → Click Edit → Load in full-screen form → Save.
  - Delete → Confirm → Trash.
  - Print → PDF with FY.
- Linking: To Inventory/Bill Desk/Ledger.
- Logic: Auto-calculate NetPayable, validate quantities, update related data.
- Fields:
  - Dhada Book: DhadaID (auto), LinkedLotID (dropdown/searchable/farmar quentity), LotDetails (JSON auto from LotEntry), FarmerName (auto), Quality (auto), SellingRecord show quality and rate-wise in table (JSON: BuyerName, Quality, Quantity, Weight, Rate, Total), CustomFields (JSON).
  - Farmer Invoice: InvoiceNo (auto), Date (date), FarmerName (dropdown/searchable), LotID dropdown/searchable/farmer quantity get inventory details: ArrivingDate, Product, farmar Quantity, transport Name, place, vehicle number.), Quality (auto), Quantity (auto), Weight (auto), Rate (auto/editable), TotalAmount (auto), Expenses (auto), Note (text), LessExpenses (auto), NetPayable (auto), CustomFields (JSON).

  - Manual Invoice: InvoiceNo (auto), Date (date), FarmerName (dropdown/searchable), LotID (dropdown/searchable/farmar quantity get inventory details: ArrivingDate, Product, farmar Quantity, transport Name, place, vehicle number. ), Quality (auto), Quantity (auto/edit), Weight (auto/edit), Rate (auto/editable), Table formate, TotalAmount (auto), Expenses: Freight (auto), advance (auto), Hamali (auto), Varai (auto), Tolai, Postage, Levy, Other - auto and editable Note (text), LessExpenses (auto), NetPayable (auto), CustomFields (JSON). Update on ledger, inventory, 

- Testing: "Generate preview for Farmer Invoice Module. Test Add/Edit/Delete/Save/Print, filter/search, auto-calculate NetPayable, Reminder toggle, PDF, Replit Database integration."

Add the fields exactly as per the prompt and get a fast build with minimal tasks.




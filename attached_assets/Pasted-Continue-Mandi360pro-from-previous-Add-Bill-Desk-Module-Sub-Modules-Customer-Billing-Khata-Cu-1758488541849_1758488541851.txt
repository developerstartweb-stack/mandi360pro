Continue Mandi360pro from previous. Add Bill Desk Module:
- Sub-Modules: Customer Billing, Khata Customer Billing, Payment Receipt.
- UI/UX: List-first table view with filter/search (minimal columns), Add button opens full-screen form (centered, clean layout), action buttons (Add Ctrl+N, Edit Ctrl+E, Delete Ctrl+D, Save Ctrl+S, Print Ctrl+P), row fade-in.
- Workflow: 
  - Add → Click Add → Full-screen form opens → Fill → Save.
  - Edit → Click Edit → Load in full-screen form → Save.
  - Delete → Confirm → Trash.
  - Print → PDF with FY.
- Linking: To Master Data/Inventory/Ledger.
- Logic: Auto-calculate totals/balances, handle cash/khata, validate quantities. Update inventory, ledger. account
- Fields:
  - Customer Billing: button to switch khata bill, BillNo (auto "cash-sv-0001"), Date (date), CustomerName (dropdown/addsearchable), select lot under quality (searchable), product name (auto), Quantity (number), weight (number average weight show suggest), Rate (number average selling rate show suggest), total. Multiple lots and products add to the bill. Commission (auto), MarketFee (auto), Hamali (auto), Total (auto), DiscountWeight (number), DiscountAmount (number),  PreviousBalance (auto from ledger), PaymentReceived (number), PaymentMode (dropdown), BalanceAmount (auto).  BillType (auto),
  - Khata Customer Billing: Same fields/logic as Customer Billing.
  -Customer Payment Receipt: button to switch khata receipt, ReceiptNo (auto "Rec-sv-001"), Date (date), Name (dropdown/add/searchable), last bill no. (link to bill, click on bill number to show bill) PendingBalance (auto), AmountReceived (number), Discount (number), BalanceAmount (auto), PaymentMode (dropdown), 
-Other Payment Receipt: for (Farmer, agent/supplier/cold storage/other. ReceiptNo (auto "Rec-sv-001"), Date (date), Name (dropdown/add/searchable), last farmer Invoice no. click on number to show) PendingBalance (auto), payable amount (number), Discount (number), BalanceAmount (auto), PaymentMode (dropdown),  

- Testing: "Generate preview for Bill Desk Module. Test Add/Edit/Delete/Save/Print, filter/search, auto-calculate (Weight*Rate/Balance auto fetch), Cash/Khata toggle, Reminder, PDF, Replit Database integration."

